// Swap this whole component for the requested app. Keep the default export named
// App, and import what you need from './ui' — see README.md.
export default function App() {
  return (
    <main className="container">
      <p className="muted">Replace this component with the requested application.</p>
    </main>
  );
}
